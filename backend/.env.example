# Environment Configuration
ENVIRONMENT=development

# Security - CRITICAL: Change these for production!
SECRET_KEY=your-super-secret-jwt-key-here-make-it-very-long-and-random-generate-with-openssl
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Database Configuration
DATABASE_URL=postgresql://postgres:secure_password_here@localhost:5432/viralos
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_RECYCLE=3600

# Redis Configuration
REDIS_URL=redis://localhost:6379

# CORS Origins (comma-separated URLs)
BACKEND_CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# AI Service API Keys - Required for AI features
OPENAI_API_KEY=sk-your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key

# Vector Database Configuration
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_ENVIRONMENT=your-pinecone-environment
PINECONE_INDEX_NAME=viralos-content

WEAVIATE_URL=http://localhost:8080
WEAVIATE_API_KEY=your-weaviate-api-key

# AWS Configuration - Required for file storage
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_S3_BUCKET=your-s3-bucket-name
AWS_REGION=us-east-1

# Social Media API Configuration
# TikTok Business API
TIKTOK_APP_ID=your-tiktok-app-id
TIKTOK_APP_SECRET=your-tiktok-app-secret
TIKTOK_REDIRECT_URI=https://your-domain.com/auth/tiktok/callback
TIKTOK_WEBHOOK_SECRET=your-tiktok-webhook-secret

# Instagram/Facebook Graph API
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
INSTAGRAM_REDIRECT_URI=https://your-domain.com/auth/instagram/callback
INSTAGRAM_WEBHOOK_VERIFY_TOKEN=your-instagram-webhook-token

# Apify Configuration - Required for TikTok scraping
APIFY_API_TOKEN=your-apify-api-token

# Optional: Performance tuning
# SCRAPING_TIMEOUT=30
# MAX_VIDEO_SIZE_MB=500
# SCRAPING_CONCURRENT_REQUESTS=8

# Instructions:
# 1. Copy this file to .env
# 2. Replace all "your-*" values with actual credentials
# 3. Generate a secure SECRET_KEY with: openssl rand -base64 32
# 4. Use strong database passwords
# 5. Never commit the .env file to version control